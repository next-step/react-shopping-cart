import { styled } from '@/stitches.config';

export const StyledOuterLayer = styled('div', {
  display: 'flex',
  justifyContent: 'center',
  width: '100%',
});

export const StyledInnerLayer = styled('div', {
  width: '60%',
  minWidth: '600px',
});
