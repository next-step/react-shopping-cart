import { PropsWithChildren } from 'react'

const CenterModalContainer = ({ children }: PropsWithChildren) => {
  return <div className="center-modal-container">{children}</div>
}
export default CenterModalContainer
