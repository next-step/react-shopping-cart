import { styled, css } from '@/stitches.config';

export const StyledOrderProductListTitle = styled('span', {
  fontSize: '18px',
  fontWeight: 'bold',
});

export const OrderStyle = css({
  padding: '20px',
  borderBottom: '1px solid rgba(0,0,0,.3)',
});
