import * as Styled from './Spinner.styles';

const Spinner = () => {
  return (
    <Styled.Container>
      <Styled.Spinner data-testid="spinner" />
    </Styled.Container>
  );
};
export default Spinner;
