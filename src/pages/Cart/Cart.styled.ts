import { styled } from '@/stitches.config';

export const StyledCartHeader = styled('header', {
  flexCenter: '',
  marginTop: '10px',
  height: '70px',
  borderBottom: '4px solid black',
  boxSizing: 'border-box',
});

export const StyledCartContentContainer = styled('div', {
  display: 'flex',
  gap: '30px',
});
